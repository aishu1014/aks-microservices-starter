grafana:
  adminPassword: "CHANGEME"   # Store via --set or SealedSecrets in real setups
  plugins:
    - grafana-image-renderer
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts: ["grafana.48.223.233.230.nip.io"]

prometheus:
  prometheusSpec:
    retention: 7d
